# OpenClaw Week 4 Homework — runtime dependencies
# Mirrors solution/pyproject.toml; use either:
#   pip install -r requirements.txt
# or
#   pip install -e ./solution[dev]

# --- Core agent runtime ---
langchain>=1.0
langchain-anthropic>=0.3
langsmith>=0.2
pydantic>=2.9,<3
python-dotenv>=1.0

# --- Test suite ---
pytest>=8
pytest-mock>=3.12
